To build:

1. Clone git@github.com:seattle-beach/weatherbus.git as a sibling of this
repo, so that it's ../../weatherbus relative to this README.
2. Follow the instructions in Weatherbus's README to make it build.
3. Run `./build`.

To run:

docker run -p 8080:8080 weatherbus
