To build:

1. git submodule update --init
2. docker build -t weatherbus/bus .

To run:

docker run -p 30001:9090 -e ONEBUSAWAY_ENDPOINT=http://api.pugetsound.onebusaway.org -e ONEBUSAWAY_KEY=test weatherbus/bus
